# ============================================================
#  Ticket Creation Plugin - Shared Build Properties
# ============================================================

plugin.name=TicketCreation
plugin.version=1.0

src.dir=src
build.dir=build
classes.dir=${build.dir}/classes
dist.dir=${build.dir}/dist

ui.dir=ui
import.dir=import
db.dir=db
lib.dir=lib